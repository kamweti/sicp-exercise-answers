( define (a-plus-abs-b a b)
        (( if (> b 0) + -) a b))

procedure accepts two parameters where if the second parameter is greater than zero

we will go ahead and add the firsxvt and second parameters, else we will subtract

the first parameter from the second
